---

# Proxmox Host
all:
  children:
      proxmox:
        hosts:
          pve.node.enso:
            ansible_host: 192.168.88.5
            ansible_user: root 

  vars:
    vm_subnet_bits: 24
    vm_gateway: 172.16.30.254